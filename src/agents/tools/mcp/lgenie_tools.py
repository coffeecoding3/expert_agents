"""
LGenie MCP 도구들
LGenie MCP 서버의 도구들을 래핑하여 내부 도구 시스템에서 사용

참고: 이 파일의 도구들은 동적으로 MCP 서버에서 로드되므로 사용하지 않음
"""

# MCP 도구들은 동적으로 MCP 서버에서 로드되므로 이 정적 클래스들은 사용하지 않음
# 모든 클래스들을 주석 처리하여 ToolRegistry에서 탐색되지 않도록 함

# import logging
# from typing import Any, Dict, Optional

# from src.capabilities.servers.external.mcp_client import mcp_manager

# logger = logging.getLogger(__name__)

# 모든 MCP 도구 클래스들이 주석 처리됨
# 이들은 동적으로 MCP 서버에서 로드됨
